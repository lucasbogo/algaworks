//Interface para agrupar os validations

package com.algaworks.algalog.domain.repository;

public interface ValidationGroups {
	
	public interface ClienteId { } //interface dentro dessa interface que serve para marcação. Por isso não possui nenhum método

}
